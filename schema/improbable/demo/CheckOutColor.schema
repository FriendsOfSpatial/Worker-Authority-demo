package improbable.demo;

type Nothing {
}

type ColorId {
	uint32 color_id = 1;
}

component CheckOutColor {
  id = 2002;
  list<uint32> colors_ids = 1;
  event Nothing ping;
  command Nothing send_color_id(ColorId);
}
